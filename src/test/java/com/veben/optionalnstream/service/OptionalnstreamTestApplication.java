package com.veben.optionalnstream.service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.veben.optionalnstream.service")
public class OptionalnstreamTestApplication {
    public static void main(String[] args) {
        SpringApplication.run(OptionalnstreamTestApplication.class, args);
    }
}